# pnpm-lock.yaml has been removed to prevent ERR_PNPM_LOCKFILE_CONFIG_MISMATCH errors during Vercel deployment.
# Vercel will now install dependencies using 'pnpm install --no-frozen-lockfile' as configured in vercel.json.
# This ensures compatibility with the multi-package workspace structure and prevents build failures.
