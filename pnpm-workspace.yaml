# This file has been removed to prevent pnpm from treating this repository as a monorepo/workspace.
# Vercel will now treat this as a standard single repository and run the build script directly
# without attempting recursive operations across workspace packages.
